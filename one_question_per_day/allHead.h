#include <stdbool.h>

int* spiralOrder(int** matrix, int matrixSize, int* matrixColSize, int* returnSize); // 顺时打印字符串
int translateNum(int num); // 把数字翻译成字符串
bool isPalindrome(int x); // 判断回文数
int* dailyTemperature(int* T, int TSize, int* returnSize); // 每日温度
int climbStairs(int n);